ROLE: Codex EXECUTOR for Magpie_M1, directed by Claude (PL). design_id=cpu_m1. Licensed EDA direct (NOT sandboxed). report-faithfully: real URG numbers only.

CONTEXT: Functional coverage = 98.61% (71/72). The ONLY uncovered bin is cg_hazard_flush:hazard:mem_stall (the one-hot 4'b0001 of {load_use_stall,muldiv_busy_stall,fetch_stall,mem_stall}). It is unreachable in the existing CORE-LOCAL harness because that harness ties the core's mem_stall input low. To hit it, mem_stall must actually assert, which only happens when cpu_m1_top (the valid/ready wrapper) sees BUS WAIT STATES.

The covergroup definitions in flow/v2_pipeline/phase_04_08_functional_coverage/cpu_m1_func_cov_bind.sv MUST NOT be changed (not one byte). The bind targets module `core`, so it auto-binds to cpu_m1_top.u_core too.

GOAL J7: add a WRAPPER-LEVEL coverage run that drives cpu_m1_top with bus wait states so mem_stall asserts, sample the func_cov bind, and MERGE its vdb with the existing 8 vdbs -> mem_stall bin hit -> 72/72 = 100%.

DO:
1. Reuse the wait-state injection pattern in flow/v2_pipeline/phase_02_01_mem_wrapper/tb_equiv.v (it drives cpu_m1_top.ibus_ready/dbus_ready with wait modes 1/3/random). Write a NEW coverage TB (e.g. tb_wrapper_func_cov.sv under phase_04_08) that:
   - instantiates cpu_m1_top with a combinational-read memory + WAIT-STATE ready generation (imode/dmode = 1 or 3 or random) loaded with an existing program (reuse a phase_04_08 .hex or the random firmware).
   - lets the func_cov bind sample (it binds to core automatically). Run long enough that a fetch/load wait asserts mem_stall with no concurrent hazard (pure 4'b0001).
   - compile with VCS coverage, emit coverage/vdb/wrapper_memstall.vdb.
2. MERGE wrapper_memstall.vdb with the existing vdbs (urg -lca -flex_merge code_union). Re-measure overall + cg_hazard_flush. Confirm mem_stall bin now HIT and overall = 100% (72/72). If for any reason mem_stall still doesn't sample as pure one-hot, report honestly with the cp_hazard sampled values seen (do NOT relax the covergroup, do NOT fake).
3. Update tests/gates/gate_04_08_functional_coverage.py threshold to the achieved floor. Keep full pytest suite green AND gate_03_08 lockstep PASS.

CONSTRAINTS: cpu_m1_func_cov_bind.sv unchanged (byte-identical). No RTL semantic change. Explicit paths.

OUTPUT (write <=30 lines) flow/v2_pipeline/phase_05_leader/J7_result.md:
  ## summary (NEW overall %; mem_stall hit? yes/no)
  ## evidence (cp_hazard sampled value for the mem_stall event; wait mode used)
  ## files_added
  ## provenance (vcs/urg, host, merge cmd)
  ## gate_status (pytest pass count; lockstep PASS?)
  ## bind_unchanged (confirm cpu_m1_func_cov_bind.sv byte-identical)
  ## tokens
