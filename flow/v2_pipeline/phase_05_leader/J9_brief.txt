ROLE: Codex EXECUTOR (gpt-5.5), directed by Claude (PL). design_id=cpu_m1. OSS (verilator/spike) + riscv-none-elf-gcc; -s danger-full-access. clean-room (no copying external cores). report-faithfully.

TASK J9: ROOT-CAUSE and FIX BUG-XBOUND-0001, then RE-VALIDATE (do NOT run the full 100k yet — PL verifies first).

BUG (see docs/reports/bug_xbound_0001/findings.md): riscv-dv seed 2026060801 — DUT & Spike agree commits 0-3 then DUT decodes a LEGAL `addi x31,x31,0xc` @pc=0x0e as illegal and traps; Spike retires it. A compressed c.li@0x4 shifts alignment to odd half-word, so pc 0x6/0xa/0xe are CONSECUTIVE cross-boundary (high-half) 32-bit instructions. Hypothesis: the lab08e residue/cross_assemble pre-fetch (IP/cpu_m1/rtl/pipeline_v2/ch2_lab08e/core.v ~lines 90-265: upcoming_cross / at_cross_boundary / cross_assemble / residue + ifu.v) handles 1 cross-boundary but does NOT set up residue for the NEXT one while consuming the current -> a RUN of cross-boundary 32-bit instrs mis-assembles the 3rd.

DO:
1. CONFIRM root cause: reproduce seed 2026060801 (flow/v2_pipeline/phase_03_09_riscvdv_lockstep, ./run_riscvdv_lockstep.py --start-seed 2026060801 --seeds 1). Add temporary internal probes (or a focused tb) to dump, at the cycle if_pc==0x0e: instr_assembled, cross_assemble, residue, cur_half_lo/hi, is_comp_*, at_cross_boundary, upcoming_cross, i_mem_rdata, i_mem_addr. Show that instr_assembled @0x0e != 0x00cf8f93 (mis-assembled). Write the evidence.
2. FIX clean-room: make the residue/pre-fetch correctly handle BACK-TO-BACK cross-boundary 32-bit instructions (each high-half 32-bit must save its high-half residue for the next as it's consumed). Keep the fix minimal + preserve the 0-cycle hot-loop behavior where possible. MUST NOT break the mem_stall freeze (mem_wrapper) logic.
3. Write ADR docs/adr/0007-crossboundary-consecutive-32bit.md (MADR; the microarch fix + why).
4. RE-VALIDATE — ALL must stay green, report each:
   - riscv-dv seed 2026060801 now matches Spike PAST commit 5 (ideally completes the program).
   - `python3 -m pytest tests/gates/gate_*.py -q` = full suite green (must stay 176 pass).
   - gate_03_08 lockstep_revalidate PASS (81-commit lockstep no regression).
   - gate_02_01 mem_wrapper equivalence PASS (cross-boundary + mem_stall interaction intact).
   - gate_04_08 functional coverage still 100% (72/72).
5. Do NOT run the full multi-seed 100k yet.

CONSTRAINTS: minimal RTL change, clean-room, ADR. Explicit paths. Keep probe scaffolding removable/clearly marked.

OUTPUT (write <=40 lines) flow/v2_pipeline/phase_05_leader/J9_result.md:
  ## root_cause (the mis-assembled bits seen @0x0e + which signal/logic is wrong)
  ## fix (files changed, what changed, ADR path)
  ## revalidate (riscv-dv seed2026060801 result; pytest pass count; gate_03_08/02_01/04_08 each PASS?)
  ## regressions (none / list)
  ## issues
  ## tokens
