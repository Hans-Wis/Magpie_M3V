READ-ONLY advisory review (you are NOT the implementer; do not edit, do not run sims). Review the changes Claude landed this session in SOC/Magpie_M1/flow/v2_pipeline/phase_03_09_riscvdv_lockstep/:
1) run_riscvdv_lockstep.py: the refactor extracting `gen_core` (parallel-safe gen, no install/restore) + `sim_compare_seed` (parallel-safe build+DUT+Spike+compare) out of generate_seed/run_one; and the Grok-safe terminal alignment replacing `dut[:_i+2]` with trim-at-own-write_tohost-exit `dut[:_i+1]`.
2) dv_farm.py: the parallel farm (install target once + build sim once -> ProcessPool of gen_and_sim -> per-seed status blackboard -> rollup).
3) the two waiver functions matched_nested_trap_waiver / watchdog_trap_waiver.
Find REAL bugs / correctness risks ONLY (not style): e.g. does the gen_core/install-once parallelization have a race or shared-state hazard? could the Grok-safe alignment ever hide a divergence or mis-trim? do the waivers ever waive a real divergence? is the spike budget / limit logic correct? any path/concurrency bug in dv_farm? Output a concise findings table: id | file:line-ish | issue | severity | why-real. If you find nothing real, say so explicitly per item.
