ROLE: Codex EXECUTOR (gpt-5.5) for Magpie_M1, directed by Claude (Project Leader). Do the work, run tools, verify. Clean-room (no copying external cores). design_id=cpu_m1, RV32IMC_Zicsr_Zifencei. You MAY use Verilator + Spike (OSS). Do NOT run licensed EDA in this job.

CONTEXT: 4-stage pipeline core at design/cpu_m1/rtl/pipeline_v2/ch2_lab08e (top for IP = cpu_m1_top.v, bare = core.v). Misalign policy = precise trap mcause 4 (load) / 6 (store), mtval=addr; a misaligned access issues NO bus request (ADR-0005). Existing lockstep harness: flow/v2_pipeline/phase_03_05_random_lockstep (tb_random_lockstep.v + random_lockstep.py vs Spike). Gates live in tests/gates/, pure pytest. platform/lib at /home/edauser/project/platform/lib (sim, spike_ref). Note: phase_03_* gates only check CACHED logs — they do NOT rebuild/re-run RTL, which let a real regression slip (misalign operator-precedence bug). 

DELIVERABLE J1 — two items:

(A) Directed MISALIGN test + gate (tests/gates/gate_02_02_misalign_trap.py):
  - Write a small RV32 program with deliberate misaligned lw (addr%4!=0), lh/sh (addr%2!=0), plus aligned lb/sb (must NOT trap). Put it under flow/v2_pipeline/phase_02_02_misalign/.
  - Run it on cpu_m1_top (Verilator) AND compare vs Spike configured to trap-on-misaligned. Assert: each misaligned access raises correct mcause (4 load / 6 store) + mtval=faulting addr; aligned byte accesses do NOT trap; NO misaligned address ever appears on the D bus (assert in tb).
  - gate asserts the above from the produced log.

(B) Re-validating lockstep gate (tests/gates/gate_03_08_lockstep_revalidate.py):
  - This gate must REBUILD the RTL from design/cpu_m1/rtl/... and RE-RUN the random Spike lockstep against the CURRENT core.v (not read a cached log). Reuse phase_03_05 harness (build with -Wno-PINMISSING; mem_stall defaults 0 for bare core). Assert per-commit match vs Spike (>=80 commits). This closes the cached-log integrity hole.

CONSTRAINTS:
- Do NOT modify RTL semantics. Do NOT touch other gates/phases. Use explicit file paths (no git add -A).
- Verify: `python3 -m pytest tests/gates/gate_*.py -q` stays fully green INCLUDING your 2 new gates.
- Keep raw logs in the phase dirs; do NOT dump them.

OUTPUT (write, <=40 lines): flow/v2_pipeline/phase_05_leader/J1_result.md with:
  ## summary (one line each: misalign test result, revalidate-lockstep result)
  ## files_added (path : purpose)
  ## evidence (mcause/mtval observed per access; lockstep commits matched)
  ## gate_status (pytest total pass count incl new gates)
  ## issues_or_waivers (anything not done / tool gaps / "none")
  ## tokens (your tokens used)
