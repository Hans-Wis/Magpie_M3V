You are the DV architect for Magpie_M1 (RV32IMC_Zicsr_Zifencei, M-mode). Write a concise Markdown plan covering TWO items. Do NOT use tools; answer from knowledge.
ITEM A — Async-interrupt lockstep (roadmap P1.1): how to exercise + lockstep M-mode interrupts (MTI/MSI/MEI via mip/mie/mstatus.MIE, mepc on interrupt entry, priority, mret) against Spike, given interrupts are non-deterministic. Cover: how to make IRQ injection DETERMINISTIC and identical on DUT (Verilator TB) and Spike (e.g. inject at a fixed retired-instruction count / fixed PC), how to compare interrupt-trap state, what riscv-dv interrupt test config to use, and the pass criteria + likely pitfalls.
ITEM B — "length-delta triage" automated gate (from an adversarial review): design a small automated check that, before any riscv-dv seed is labeled pass or "benign", extracts the last ~10 commits on both DUT and Spike around any commit-count delta and emits a one-line root-cause classification (e.g. nested-trap-resume, early-tohost-bad-branch, handler-length-diff). Specify the exact data to capture and the decision rules. Keep concrete.
Output clean Markdown.
